{
  "name": "poker-buy-in",
  "private": true,
  "version": "1.0.0",
  "description": "Poker buy-in tracker",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
